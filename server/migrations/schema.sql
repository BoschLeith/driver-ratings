
--
-- LibSQL SQL Schema dump automatic generated by geni
--

CREATE TABLE schema_migrations (id VARCHAR(255) NOT NULL PRIMARY KEY);
CREATE TABLE teams (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, full_name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT NULL);
CREATE TABLE sqlite_sequence(name,seq);
CREATE TABLE races (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, race_date DATE NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT NULL);
CREATE TABLE raters (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT NULL);
CREATE TABLE ratings (id INTEGER PRIMARY KEY AUTOINCREMENT, driver_id INTEGER NOT NULL, race_id INTEGER NOT NULL, rater_id INTEGER NOT NULL, rating REAL CHECK (rating >= 0.0 AND rating <= 10.0), created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT NULL, FOREIGN KEY (driver_id) REFERENCES Drivers (id) ON DELETE CASCADE, FOREIGN KEY (race_id) REFERENCES Races (id) ON DELETE CASCADE, FOREIGN KEY (rater_id) REFERENCES Raters (id) ON DELETE CASCADE);
CREATE TABLE drivers (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT NULL);
CREATE TABLE driver_team (driver_id INTEGER NOT NULL, team_id INTEGER NOT NULL, season INTEGER NOT NULL, PRIMARY KEY (driver_id, team_id, season), FOREIGN KEY (driver_id) REFERENCES drivers (id) ON DELETE CASCADE, FOREIGN KEY (team_id) REFERENCES teams (id) ON DELETE CASCADE);