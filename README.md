# Driver Ratings

This project is a full-stack application built with React, TypeScript, Vite, and Express. It allows users to view and interact with driver ratings.

## Project Structure

The project is divided into two main parts:

- `client/`: The frontend application built with React and Vite.
- `server/`: The backend application built with Express and TypeScript.
